import re
from pathlib import Path

from core.models import Vacancy

STOPWORDS_PATH = Path(__file__).resolve().parent.parent / "config" / "stopwords.txt"

TARGET_CITY_MARKERS = ["нижний новгород", "нижегородск"]

# Вайтлист-исключение: стоп-слово не отсеивает вакансию, если в названии
# одновременно есть один из этих целевых маркеров.
WHITELIST_STEMS = [
    "оценк", "развити", "обучени", "t&d", "l&d", "talent", "hrbp", "бизнес-партнёр",
    "корпоративн университет", "ассессмент", "assessment", "компетенц", "адаптаци",
    "кадровый резерв", "performance",
]

# Фильтр релевантности: вакансия должна относиться к направлению.
# Без него в дайджест попадает любая удалённая работа — Go-разработчики,
# тестировщики, SMM, — потому что стоп-слова их не ловят, а гео пропускает.
#
# Маркеры разделены на два класса. Сильные однозначно указывают на HR-профессию
# и срабатывают сами по себе. Слабые сами по себе не значат ничего: «развитие»
# бывает у соцсетей, «обучение» — у нейросети, «бизнес-партнёр» — финансовый,
# поэтому им нужен HR-контекст рядом.
STRONG_STEMS = [
    "hr", "эйчар", "персонал", "human resources", "people partner", "hrbp",
    "t&d", "l&d", "ассессмент", "assessment", "кадровый резерв", "c&b",
    "корпоративн университет", "корпоративн культур", "рекрутм", "тренинг",
]

# Названия должностей, где слабый маркер стоит в роли профессии. Без них
# терялись «Специалист по обучению» и «Специалист по оценке» — HR-контекста
# в названии нет, а вакансия целевая.
#
# В отличие от STRONG_STEMS, эти сочетания отменяются чужой предметной областью:
# «специалист оценк» ловил «Специалиста по финансово-экономической оценке».
#
# «специалист развити» и «менеджер развити» сюда не добавлять: они ловят
# «менеджера по развитию малого бизнеса» и «по развитию сделок». Целевые
# названия проходят как слабый маркер вместе с HR-контекстом.
ROLE_COMBOS = [
    "обучени развити", "специалист обучени", "менеджер обучени",
    "руководител обучени", "директор обучени", "начальник обучени",
    "отдел обучени", "центр обучени", "специалист оценк", "менеджер оценк",
    "руководител оценк",
]

WEAK_STEMS = [
    "оценк", "развити", "обучени", "talent", "бизнес-партнёр", "компетенц",
    "адаптаци", "performance", "тренер", "методист", "наставни", "карьерн",
    "мотиваци", "внутренн коммуникац",
]

# Чужие предметные области. Если такое слово стоит в названии, сочетание вроде
# «специалист оценк» или «менеджер обучени» относится не к персоналу:
# «специалист по финансово-экономической оценке», «эксперт по обучению ИИ».
FOREIGN_DOMAIN = [
    "финанс", "экономическ", "недвижим", "имуществ", "кредит", "залог",
    "ущерб", "актив", "сделок", "соцсет", "контент", "искусственн",
    "малого бизнеса", "продаж", "маркетинг", "реклам",
]

# Контекст, который превращает слабый маркер в релевантный.
HR_CONTEXT = [
    "персонал", "сотрудник", "hr", "команд", "талант", "руководител",
    "кадр", "люде", "people", "employee", "работник",
]

# Ручной подбор. Владельцу интересно руководить процессом найма и иногда
# собеседовать точечные позиции, но не закрывать вакансии руками как основную
# работу. По названию это неразличимо: «HR-менеджер» и «Менеджер по персоналу»
# бывают и тем, и другим, — поэтому смотрим на обязанности в тексте поста.
RECRUITING_DUTIES = [
    "полный цикл подбора", "цикл подбора", "активный поиск", "искать активно",
    "поиск кандидатов", "поиск специалистов", "закрытие вакансий",
    "закрыть любую позицию", "закрытые роли", "план по набору", "плана по набору",
    "план по подбору", "закрытую вакансию", "ведение вакансий", "вакансий в работе",
    "телефонное интервью", "телефонных интервью", "звонков в день",
    "первичный скрин", "скрининг", "шорт-лист", "сорсинг", "массовый подбор",
    "воронка подбора", "опыт в подборе", "опыт по набору", "опыт найма",
    "составление вакансии", "размещения вакансий", "продажа вакансии",
    "источников привлечения", "источников размещения", "проведение интервью",
    "проведение собеседований", "подбор персонала", "набору персонала",
    "поиска до выхода",
]

# Признаки того, что подбором руководят, а не занимаются руками.
RECRUITING_OVERSIGHT = [
    "курир", "контроль процесса", "управлять", "управление командой",
    "руководство командой", "hr-команд", "hr команд", "стратег", "выстро",
    "построени процесс", "с нуля", "аудит", "метрик", "трансформац", "грейд",
]

# Два и более маркера — подбор занимает заметную часть работы. Порог выбран
# по живым данным: у вакансий, которые владелец забраковал, маркеров 2-9;
# у тех, что оставил (Head of HR, HR Director), — ни одного.
MAX_RECRUITING_DUTIES = 1
# Столько признаков руководства оправдывают упоминания подбора.
MIN_OVERSIGHT_TO_KEEP = 2

# Резюме соискателей: часть каналов смешивает их с вакансиями в одной ленте.
RESUME_MARKERS = [
    "нахожусь в поиске работы", "ищу работу", "ищу удалённую работу",
    "ищу удаленную работу", "рассматриваю предложения", "рассмотрю предложения",
    "моё резюме", "мое резюме", "резюме прилагаю", "открыт к предложениям",
    "открыта к предложениям", "в активном поиске",
]


def _load_stopwords() -> list[str]:
    if not STOPWORDS_PATH.exists():
        return []
    stems = []
    for line in STOPWORDS_PATH.read_text(encoding="utf-8").splitlines():
        line = line.strip()
        if line and not line.startswith("#"):
            stems.append(line.lower())
    return stems


def _stem_matches(stem: str, text: str) -> bool:
    """Стем — основа слова, совпадение считается от начала слова.

    Без привязки к границе слова короткие латинские основы дают ложные срабатывания:
    "hr" находится внутри "through", "l&d" — внутри ссылок.

    Стем может состоять из нескольких слов (например "корпоративн университет") —
    тогда каждое слово должно встретиться в тексте, не обязательно подряд."""
    return all(
        re.search(rf"(?<!\w){re.escape(word)}", text) is not None
        for word in stem.split()
    )


# Маркеры того, что сообщение вообще является вакансией.
# Изначально задумывались для чатов, но каналы тоже несут рекламу, промо-посты
# и личную переписку, поэтому предфильтр применяется ко всем текстовым источникам.
#
# Здесь только однозначные признаки найма. Общие слова из списка убраны:
# «ищет» нашлось внутри «вы ищете новое место» в редакционной статье,
# «зарплата» и «условия» встречаются в любом тексте про работу. Одного общего
# маркера достаточно, чтобы статья про карьеру прошла за вакансию.
VACANCY_MARKERS = [
    "вакансия", "#вакансия", "вакансии нет", "ищем", "в команду",
    "открыта позиция", "открыт конкурс", "вилка", "откликнуться", "отклик",
    "резюме", "требуется", "приглашаем", "обязанности", "задачи:",
    "требования:", "условия:", "компания:", "мы предлагаем", "что мы предлагаем",
    "график работы", "оклад", "з/п", "испытательный срок", "трудоустройство",
    "занятость", "формат работы", "локация:", "уровень дохода",
]

# Реклама: подборки каналов, промо конференций, оплаченные посты. Формально
# они содержат маркеры вакансии — в описании чужого канала легко встречается
# «резюме» или «вакансии», — но вакансией не являются.
ADVERT_MARKERS = [
    "лучшие telegram-каналы", "лучшие телеграм-каналы", "подборка каналов",
    "собрали для вас", "полезные каналы", "рекомендуем каналы",
    "подписывайтесь на канал", "реклама.", "erid",
]

# Подборка каналов упоминает слово «канал» столько раз, сколько в ней пунктов.
# У настоящей вакансии повода сделать это трижды практически нет.
MAX_CHANNEL_MENTIONS = 2

# Короткие сообщения — почти всегда реплики в обсуждении, а не вакансия.
MIN_VACANCY_LENGTH = 200


def vacancy_marker_filter(vacancies: list[Vacancy]) -> list[Vacancy]:
    """Отсеивает сообщения, не похожие на вакансию.

    Применяется только к источникам с неструктурированным текстом (Telegram):
    у вакансий с сайтов и из API raw_text пустой, их пропускаем как есть."""
    result = []
    for v in vacancies:
        if not v.raw_text:
            result.append(v)
            continue
        if len(v.raw_text) < MIN_VACANCY_LENGTH:
            continue
        lowered = v.raw_text.lower()
        if any(marker in lowered for marker in ADVERT_MARKERS):
            continue
        if lowered.count("канал") > MAX_CHANNEL_MENTIONS:
            continue
        if any(marker in lowered for marker in VACANCY_MARKERS):
            result.append(v)
    return result


def geo_filter(vacancies: list[Vacancy]) -> list[Vacancy]:
    result = []
    for v in vacancies:
        if v.remote:
            result.append(v)
            continue
        city = (v.city or "").lower()
        if any(marker in city for marker in TARGET_CITY_MARKERS):
            result.append(v)
    return result


def stopword_filter(vacancies: list[Vacancy], stopwords: list[str] | None = None) -> list[Vacancy]:
    stopwords = _load_stopwords() if stopwords is None else stopwords
    result = []
    for v in vacancies:
        title = v.title.lower()
        hit_stopword = any(_stem_matches(stem, title) for stem in stopwords)
        if not hit_stopword or any(_stem_matches(stem, title) for stem in WHITELIST_STEMS):
            result.append(v)
    return result


def relevance_filter(vacancies: list[Vacancy]) -> list[Vacancy]:
    """Оставляет только вакансии по направлению «оценка и развитие персонала».

    Маркер ищется строго по названию, как и стоп-слова. По всему тексту поста
    искать нельзя: почти любая вакансия упоминает «обучение» и «мотивацию»
    в разделе условий, и тогда фильтр пропускает вообще всё.

    Слабый маркер засчитывается только вместе с HR-контекстом: иначе проходят
    «ассистент по развитию соцсетей» и «эксперт по обучению ИИ»."""
    result = []
    for v in vacancies:
        title = v.title.lower()
        if any(_stem_matches(stem, title) for stem in STRONG_STEMS):
            result.append(v)
            continue
        foreign = any(_stem_matches(stem, title) for stem in FOREIGN_DOMAIN)
        if any(_stem_matches(stem, title) for stem in ROLE_COMBOS) and not foreign:
            result.append(v)
            continue
        weak_hit = any(_stem_matches(stem, title) for stem in WEAK_STEMS)
        context_hit = any(_stem_matches(stem, title) for stem in HR_CONTEXT)
        if weak_hit and context_hit:
            result.append(v)
    return result


def recruiting_filter(vacancies: list[Vacancy]) -> list[Vacancy]:
    """Отсеивает вакансии, где подбор персонала — основная работа.

    Проверяется по тексту поста, а не по названию: «HR-менеджер» бывает и
    руководителем HR-функции, и рекрутёром на потоке. Вакансия, где подбор
    только упоминается в контексте руководства процессом, остаётся."""
    result = []
    for v in vacancies:
        if not v.raw_text:
            result.append(v)
            continue
        lowered = v.raw_text.lower()
        duties = sum(1 for m in RECRUITING_DUTIES if m in lowered)
        oversight = sum(1 for m in RECRUITING_OVERSIGHT if m in lowered)
        if duties > MAX_RECRUITING_DUTIES and oversight < MIN_OVERSIGHT_TO_KEEP:
            continue
        result.append(v)
    return result


def resume_filter(vacancies: list[Vacancy]) -> list[Vacancy]:
    """Отсеивает резюме соискателей.

    Часть каналов публикует вакансии и резюме в одной ленте, а предфильтр
    по маркерам вакансии их не различает: «в поиске» и «резюме» есть и там,
    и там. Ищем по началу текста — резюме представляются в первых строках."""
    result = []
    for v in vacancies:
        head = f"{v.title}\n{v.raw_text[:400]}".lower()
        if not any(marker in head for marker in RESUME_MARKERS):
            result.append(v)
    return result


# Порядок шагов конвейера и человеческие названия причин отсева.
# Единственное место, где он задан: и apply_filters, и review.py берут его
# отсюда. Раньше список был продублирован, и review.py молча отстал на два
# фильтра, показывая неверную картину.
FILTER_STAGES = [
    ("не похоже на вакансию", vacancy_marker_filter),
    ("резюме соискателя", resume_filter),
    ("не по направлению", relevance_filter),
    ("подбор как основная работа", recruiting_filter),
    ("не тот регион", geo_filter),
    ("стоп-слово в названии", stopword_filter),
]


def apply_filters(vacancies: list[Vacancy]) -> list[Vacancy]:
    """Прогоняет вакансии по всем шагам в порядке FILTER_STAGES."""
    for _, stage in FILTER_STAGES:
        vacancies = stage(vacancies)
    return vacancies
